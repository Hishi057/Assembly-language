# ニーモニック メモ

## if文

`cmp A B` 内部的に`B-A`を計算
`je (ラベル)` `A=B`であればラベルに飛ぶ
`jne (ラベル)` `A!=B`であれば
`jge (ラベル)` `A<=B`であれば
`jnge (ラベル)` `A>B`であれば

## 特殊

`loop (ラベル)` `%rcx`が0じゃなければラベルの部分に飛ばして、かつ`%rcx`を1減らす。でも、3つ目の返り値と被るし、if文使った方が早いしでなんでこんなのがあるんだと思ったらまあ昔の名残らしい。AI情報だけど。
